# PlugPark v0.4 — API Data Layer Rebuild

## 목표
API 응답 필드명을 추측해서 읽는 방식을 제거하고, 실제 명세/실응답에 맞춘 DTO → 내부 모델 → JOIN 순서로 정리한다.

## 주차 API

### 부산광역시 공영주차장 기본정보
- `mgntNum`: 관리번호
- `pkNam`: 주차장명
- `doroAddr`, `jibunAddr`: 주소
- `pkCnt`: 주차구획수
- `xCdnt`, `yCdnt`: 위도/경도
- `guNm`: 관리기관
- `currava`: 기존 API의 실시간주차면수(보조값)

### 부산시설공단 실시간 주차
실제 응답 필드를 그대로 사용한다.
- `parkgcd`
- `parknm`
- `curravacnt`
- `parkingcnt`
- `maxcnt`
- `lastupdatetime`

실시간 API에는 주소/좌표가 없어도 `parknm`을 정규화하여 기본 주차 API의 `pkNam`과 JOIN한다.

JOIN 우선순위:
1. 정규화 이름 완전일치
2. 포함관계(충분한 이름 길이 필요)
3. Dice 문자열 유사도 0.84 이상 + 2위 후보와 0.08 이상 차이
4. 후보가 여러 개면 `ambiguous`로 남기고 자동 확정하지 않음

## EV API

### getChargerInfo
공식 필드 `statNm`, `statId`, `chgerId`, `chgerType`, `addr`, `lat`, `lng`, `stat`, `statUpdDt`, `output`, `zcode`, `zscode`, `delYn` 등을 정확히 읽는다.

- 항상 `zcode=26`
- `dataType=JSON`
- 사용자 요청에서는 외부 EV API를 기다리지 않는다.
- Cloudflare Cache에 200건씩 2페이지 단위로 백그라운드 수집한다.
- 첫 페이지에서 부산 비율이 80% 미만이면 `regionFilterHonored=false`로 진단하고 전국 전체 스캔을 하지 않는다.

### getChargerStatus
- `statId + chgerId` 복합키로 Info에 덮어쓴다.
- 최근 10분 변경 상태만 별도 캐시에 보관한다.
- Status API 실패는 사용자 화면 실패로 전파하지 않는다.

## EV ↔ 주차장 매칭
거리만 가까운 충전소를 붙이지 않는다.

우선순위:
1. 정규화 이름 완전일치
2. 이름 포함 관계 + 거리 제한
3. 높은 이름 유사도 + 거리 제한
4. 가까운 거리 + 주소 유사도

따라서 근처 아파트/기관 충전기가 공영주차장에 잘못 붙는 false positive를 줄인다.

## 진단 API
- `/api/diagnostics/parking`
- `/api/diagnostics/parking-realtime`
- `/api/diagnostics/matching`
- `/api/diagnostics/ev-info`
- `/api/diagnostics/ev-status`
- `/api/ev-search?q=센텀`
- `/api/ev-summary`

각 진단은 예상 필드, 실제 첫 item key, valid/invalid 건수 등을 반환한다.
